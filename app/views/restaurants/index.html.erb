<h1>All Our Restaurants :</h1>

<table>
  <tbody>
    <% @restaurants.each do |r| %>
    <tr>

      <td><%= r.name %></td>
      <td><%= link_to "Show", restaurant_path(r), class: "btn btn-primary" %></td>
 <!--      <td><%#= link_to "Edit", edit_restaurant_path(r) %> |</td>
      <td><%#= link_to "Destroy", restaurant_path(r), method: "delete", data:
      { confirm: "Are you sure?" } %></td> -->
    </tr>
    <% end %>
  </tbody>
</table>
