<h1>Ajouter Reviews a <%= @restaurant.name %></h1>

<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: [1, 2, 3, 4, 5] %>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>
